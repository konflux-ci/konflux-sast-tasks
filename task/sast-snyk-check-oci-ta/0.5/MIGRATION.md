# Migration from 0.4 to 0.5

Version 0.5:

- The `product-name` flag is being enforced. If not passed by user, it will be set using the `PRODUCT_NAME` environmanet variable.
## Action from users
- None. 
 
