---
base: ../../sast-gitleaks-check/0.1/sast-gitleaks-check.yaml
add:
  - use-source
  - use-prefetch
preferStepTemplate: true
removeWorkspaces:
  - workspace
replacements:
  workspaces.workspace.path: /var/workdir
regexReplacements:
  hacbs/\$\(context.task.name\): source
useTAVolumeMount: true
